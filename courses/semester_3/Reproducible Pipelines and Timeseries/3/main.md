1) try direct links, e.g. input -> lstm -> output, input -> output connection!
    - $\hat{y} = w_1a_1 + w_2a_2 + \phi x_1 + \phi x_2$
    - $a$ non linear
    - $\phi$ linear.
    - skip connection from input to output.