# Clustering

Use one clustering method (e.g. DBSCAN) to identify the number of clusters and then use something like K-Means to draw those clusters. In general, combine multiple algorithms together, but for different purposes.